# Test-only dependencies (not shipped in the frozen backend.exe).
#
# Install with:  pip install -r requirements.txt -r requirements-test.txt
# Run with:      python -m pytest tests/
pytest>=8.0.0

# Required by fastapi.testclient / starlette.testclient. Without it the
# endpoint-level tests (which assert /setup/diagnostics returns 200 rather
# than the 404 that shipped in 1.3.1) cannot run at all.
httpx>=0.27.0
